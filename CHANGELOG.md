# 版本记录

## v1.0.3

- 手机横屏（如 iPhone 14 的 844×390）此前因屏宽超过 820px 断点而回落到桌面侧栏布局，侧栏在 390px 高的视口里放不下，底部用户区被裁切；现在按「粗指针 + 矮视口」一并判定为移动端，并补了一套横屏压缩规则（顶栏 / 标签栏压薄、弹窗改回居中卡片、栅格恢复多列）
- 触屏点击热区整体抬高：勾选框 22px（命中区 36px）、行内图标按钮 36px、顶栏搜索与账号按钮 38px、分段切换与筛选 chip 40px、热力图区间按钮 36px；此前普遍在 26~35px，手机上好点空、易误触相邻项
- 手机日历改为「日期 + 圆点 + 选中日清单」：45px 宽的格子里文字只能露出两个字，现改为圆点表示有无待办，点格子后在网格下方展开该日完整清单（44px 行高、可点即切换完成）；桌面端文字条目与拖拽行为不变
- 修复统计页移动端样式被覆盖：该段媒体查询写在基础样式之前，同权重下被后者反超，导致 365 天热力图的横向滚动（`.heat-body` 的 `min-width: 560px`）从未生效、区间切换按钮也没加高；已移至样式末尾
- 新增 PWA 支持：补 manifest 与 192/512/apple-touch/maskable 图标，手机可「添加到主屏幕」后以独立应用形态打开

## v1.0.2

- 行动与待办支持拆分子项：行内「☑」展开子列表，子项可勾选完成、双击改名、拖动排序、删除；子项只有名字 / 完成状态 / 排序，不带番茄钟、优先级与日期，也不参与统计
- 绑定了行动的待办与其行动共用同一份子项：项目列表（行动）与 todo 列表（待办）双向同步，任一侧的增删改与排序都会同步；删除行动 / 待办时级联清理子项
- 列表内联改名：双击名称就地编辑（待办、行动、首页「今日到期行动」）
- 备份 / 恢复与历史数据迁移支持子项（备份新增 `checklist` 段，不含该段的旧备份照常导入）

## v1.0.1

- 新增区域管理：区域支持排序、编辑、删除和自定义颜色
- 项目列表统一排序：进行中的在前、按优先级和日期排列，首页摘要同步修正
- 工时统计修正：番茄钟完成的工时（含非默认时长）全部计入统计和热力图
- 创建行动时开始日期改为可选，未排期的待办进入置顶的「未分配」分组，可拖拽到具体日期

## v1.0.0

「进行时工作台」首个完整版本：个人事业规划应用。

- 区域 → 项目 → 任务 → 行动的四级规划结构，配套待办、番茄钟、工时、笔记
- 多用户注册登录，数据按账号隔离，支持游客模式体验
- 双运行模式：本地 H2 与云端数据库（多端同步）
- 进行中、仪表盘、待办、统计、日历、设置六大视图，支持备份与恢复
